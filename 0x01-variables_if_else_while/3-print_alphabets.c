#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - Entry 
 *Action - Prints the alphabet.
 * Return: Always 0 (Success)
 */

int main(void)
{	

	char low;
	for (low = 'a'; low <= 'z'; low++)
	purchar(low);

	for (low 'A'; low <= 'z'; low++
	purchar(low);

	putchar('\n');
	return (0);
}	

